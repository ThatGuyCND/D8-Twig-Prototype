(function ($) {
  var w = window,
      wH = $(w).height(),
      wW = $(w).width(),
      d = document,
      dH = $(d).height(),
      bgs = $('.background-elements'),
      lGrps = bgs.find('.light--pair'),
      l = bgs.find('.light');
  
  // w.addEventListener ("resize", function () {
  //   var wH = $(w).height(),
  //       wW = $(w).width();
  // });
  
  // if (navigator.userAgent.toLowerCase().indexOf('firefox') > -1) {} else {}

})(jQuery);